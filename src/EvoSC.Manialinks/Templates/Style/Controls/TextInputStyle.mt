<component>
  <template>
    <stylesheet>
      <style
              class="txtinput-text"
              textcolor="{{ Theme.UI_TextPrimary }}"
              textfont="{{ Font.Regular }}"
              textsize="{{ Theme.UI_FontSize }}"
      />
    </stylesheet>
  </template>
</component>
